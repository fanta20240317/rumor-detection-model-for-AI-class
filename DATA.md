# explanation for csv

- `id`: unique item identifier
- `text`: claim or post text
- `label`: `1 = rumor`, `0 = non-rumor`
- `event`: optional event||source grouping field

`val.csv` is a validation set.
